# child_runs_on_a_pty

on an interactive terminal worldline runs the child on a real PTY, so the child sees a TTY on stdin/stdout/stderr

## `worldline --dump dump -- vtt check-tty`

```
stdin:tty
stdout:tty
stderr:tty
```
